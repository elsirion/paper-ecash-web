[build]
dist = "dist"
target = "crates/paper-ecash-web/index.html"
filehash = false
minify = "on_release"

[watch]
watch = ["crates/paper-ecash-web", "public"]

# Uncomment for release builds — slows dev rebuilds by ~60s
# [[hooks]]
# stage = "post_build"
# command = "wasm-opt"
# command_arguments = ["-Oz", "--all-features", "-o", "dist/.stage/paper-ecash-web_bg.wasm", "dist/.stage/paper-ecash-web_bg.wasm"]

[serve]
addresses = ["127.0.0.1"]
port = 8080
open = false
